import 'package:flutter/material.dart';

class FoodContainerWidget extends StatelessWidget {
  const FoodContainerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 200,
    );
  }
}
